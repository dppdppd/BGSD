#+TITLE: BGSD Future
#+TODO: TODO IN-PROGRESS BLOCKED | DONE

* 0.5.x polish
** TODO Dark/light theme toggle
** TODO Clean up unused .toggle-btn.disabled CSS selector
** TODO Pre-flight check in build-release.sh for wine + 7z + i386 arch on Linux hosts
   The script silently aborted Windows + macOS builds when wine wasn't installed (set -e short-circuit). A leading dependency probe with a clear error would save a 5-minute false start.
** TODO Sign Windows .exe + macOS .zip release artifacts
   Currently ships unsigned. Windows: optional code-signing cert + signtool. macOS: requires macOS host for codesign + notarization.
