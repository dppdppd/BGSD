wait app-root
new bit
js (() => { const inputs = document.querySelectorAll('.block-defaults input[value="all"]'); if (!inputs.length) throw new Error("block default controls not found"); inputs.forEach((input) => { if (!input.checked) input.click(); }); return `${inputs.length} block default controls set to all`; })()
js (() => { const getRow = (id) => { const row = document.querySelector(`[data-testid="${id}"]`); if (!row) throw new Error(`${id} row not found`); return row; }; const global = getRow("virtual-G_PRINT_GROUPS"); const globalButton = document.querySelector('[data-testid="dynamic-ref-global-G_PRINT_GROUPS"]'); if (!globalButton || !/All/.test(globalButton.textContent || "")) throw new Error(`G_PRINT_GROUPS should start as All, got ${globalButton?.textContent}`); if (global.querySelector("select.kv-type-select")) throw new Error("G_PRINT_GROUPS should use the dynamic selector"); const getInput = (id) => { const row = getRow(id); const mode = row.querySelector("select.kv-type-select"); if (!mode || mode.value !== "text") throw new Error(`${id} should start as text mode, got ${mode?.value}`); const input = row.querySelector("input.kv-str"); if (!input) throw new Error(`${id} text input not found`); return input; }; const box = getInput("virtual-PRINT_GROUP"); const lid = getInput("virtual-lid-PRINT_GROUP"); if (box.value.includes('"')) throw new Error(`box PRINT_GROUP displayed quotes: ${box.value}`); if (lid.value.includes('"')) throw new Error(`lid PRINT_GROUP displayed quotes: ${lid.value}`); return "print group controls render without SCAD quotes"; })()
shot print_group_text_fields_empty
js (async () => { const wait = () => new Promise((r) => setTimeout(r, 150)); const rowsByLabel = (label) => [...document.querySelectorAll(".line-row.kv")].filter((row) => row.querySelector(".kv-key")?.textContent.trim() === label); const rowFor = (id, occurrence) => document.querySelector(`[data-testid="${id}"]`) || rowsByLabel("Print Group")[occurrence]; const setInput = async (id, occurrence, value) => { let row = rowFor(id, occurrence); if (!row) throw new Error(`${id} row not found`); const select = row.querySelector("select.kv-type-select"); if (select && select.value !== "text") { select.value = "text"; select.dispatchEvent(new Event("change", { bubbles: true })); await wait(); row = rowFor(id, occurrence); } const input = row?.querySelector("input.kv-str"); if (!input) throw new Error(`${id} text input not found`); input.value = value; input.dispatchEvent(new Event("change", { bubbles: true })); await wait(); }; const click = async (id) => { const el = document.querySelector(`[data-testid="${id}"]`); if (!el) throw new Error(`${id} not found`); el.click(); await wait(); }; await setInput("virtual-PRINT_GROUP", 1, "box-group"); await setInput("virtual-lid-PRINT_GROUP", 2, "lid-group"); await click("dynamic-ref-global-G_PRINT_GROUPS"); await click("dynamic-ref-global-G_PRINT_GROUPS-term-box-group"); await click("dynamic-ref-global-G_PRINT_GROUPS-term-lid-group"); const quotedInputs = [...document.querySelectorAll(".line-row.kv input.kv-str")].filter((input) => /^".*"$/.test(input.value)); if (quotedInputs.length) throw new Error(`quoted text input values found: ${quotedInputs.map((input) => input.value).join(", ")}`); return "print group values remain bare in text inputs"; })()
shot print_group_text_fields_values
scad
js (() => { const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ""; if (!scad.includes('[ G_PRINT_GROUPS, ["box-group", "lid-group"] ]')) throw new Error("missing selected G_PRINT_GROUPS output"); if (!scad.includes('[ PRINT_GROUP, "box-group" ]')) throw new Error("missing quoted box PRINT_GROUP output"); if (!scad.includes("BOX_LID")) throw new Error("missing materialized BOX_LID output"); if (!scad.includes('[ PRINT_GROUP, "lid-group" ]')) throw new Error("missing quoted lid PRINT_GROUP output"); return "print groups are quoted in generated SCAD"; })()
