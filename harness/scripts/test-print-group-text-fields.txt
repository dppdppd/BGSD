wait app-root
new bit
click view-menu-button
click view-defaults-all
js (() => { const getInput = (id) => { const row = document.querySelector(`[data-testid="${id}"]`); if (!row) throw new Error(`${id} row not found`); const input = row.querySelector("input.kv-str"); if (!input) throw new Error(`${id} text input not found`); return input; }; const box = getInput("virtual-PRINT_GROUP"); const lid = getInput("virtual-lid-PRINT_GROUP"); if (box.value.includes('"')) throw new Error(`box PRINT_GROUP displayed quotes: ${box.value}`); if (lid.value.includes('"')) throw new Error(`lid PRINT_GROUP displayed quotes: ${lid.value}`); return "print group text inputs render without SCAD quotes"; })()
shot print_group_text_fields_empty
js (async () => { const wait = () => new Promise((r) => setTimeout(r, 150)); const setInput = async (id, value) => { const row = document.querySelector(`[data-testid="${id}"]`); if (!row) throw new Error(`${id} row not found`); const input = row.querySelector("input.kv-str"); if (!input) throw new Error(`${id} text input not found`); input.value = value; input.dispatchEvent(new Event("change", { bubbles: true })); await wait(); }; await setInput("virtual-G_PRINT_GROUP", "global-group"); await setInput("virtual-PRINT_GROUP", "box-group"); await setInput("virtual-lid-PRINT_GROUP", "lid-group"); const quotedInputs = [...document.querySelectorAll(".line-row.kv input.kv-str")].filter((input) => /^".*"$/.test(input.value)); if (quotedInputs.length) throw new Error(`quoted text input values found: ${quotedInputs.map((input) => input.value).join(", ")}`); return "print group values remain bare in text inputs"; })()
shot print_group_text_fields_values
scad
js (() => { const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ""; if (!scad.includes('[ G_PRINT_GROUP, "global-group" ]')) throw new Error("missing quoted G_PRINT_GROUP output"); if (!scad.includes('[ PRINT_GROUP, "box-group" ]')) throw new Error("missing quoted box PRINT_GROUP output"); if (!scad.includes("BOX_LID")) throw new Error("missing materialized BOX_LID output"); if (!scad.includes('[ PRINT_GROUP, "lid-group" ]')) throw new Error("missing quoted lid PRINT_GROUP output"); return "print groups are quoted in generated SCAD"; })()
