wait app-root
new bit
click view-menu-button
click view-defaults-all
js (() => { const row = document.querySelector('[data-testid="virtual-G_PRINT_DIVIDERS"]'); if (!row) throw new Error("G_PRINT_DIVIDERS row not found"); const select = row.querySelector("select.kv-type-select"); if (!select) throw new Error("mixed-type selector not found"); if (select.value !== "true") throw new Error(`G_PRINT_DIVIDERS should start as true, got ${select.value}`); if (row.querySelector("input.kv-str")) throw new Error("boolean mode should not show a text input"); return "mixed-type boolean mode is explicit"; })()
shot mixed_type_bool_mode
js (async () => { const wait = () => new Promise((r) => setTimeout(r, 150)); const rowsByLabel = (label) => [...document.querySelectorAll(".line-row.kv")].filter((row) => row.querySelector(".kv-key")?.textContent.trim() === label); const rowFor = (id, label) => document.querySelector(`[data-testid="${id}"]`) || rowsByLabel(label)[0]; let row = rowFor("virtual-G_PRINT_DIVIDERS", "Print Dividers"); let select = row?.querySelector("select.kv-type-select"); if (!select) throw new Error("G_PRINT_DIVIDERS selector not found"); select.value = "false"; select.dispatchEvent(new Event("change", { bubbles: true })); await wait(); row = rowFor("virtual-G_PRINT_DIVIDERS", "Print Dividers"); select = row?.querySelector("select.kv-type-select"); if (!select || select.value !== "false") throw new Error(`expected false mode, got ${select?.value}`); if (row.querySelector("input.kv-str")) throw new Error("false mode should not show a text input"); select.value = "list"; select.dispatchEvent(new Event("change", { bubbles: true })); await wait(); row = rowFor("virtual-G_PRINT_DIVIDERS", "Print Dividers"); select = row?.querySelector("select.kv-type-select"); if (!select || select.value !== "list") throw new Error(`expected list mode, got ${select?.value}`); let input = row?.querySelector("input.kv-str"); if (!input) throw new Error("list mode text input not found"); input.value = "red, blue"; input.dispatchEvent(new Event("change", { bubbles: true })); await wait(); row = rowFor("virtual-G_LID_THICKNESS", "Lid Thickness"); select = row?.querySelector("select.kv-type-select"); if (!select || select.value !== "false") throw new Error(`G_LID_THICKNESS should start false, got ${select?.value}`); select.value = "number"; select.dispatchEvent(new Event("change", { bubbles: true })); await wait(); row = rowFor("virtual-G_LID_THICKNESS", "Lid Thickness"); input = row?.querySelector("input.kv-num"); if (!input) throw new Error("number mode numeric input not found"); input.value = "2.5"; input.dispatchEvent(new Event("change", { bubbles: true })); await wait(); return "mixed-type controls switch between booleans, lists, and numbers"; })()
shot mixed_type_list_mode
scad
js (() => { const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ""; if (!scad.includes('[ G_PRINT_DIVIDERS, ["red", "blue"] ]')) throw new Error("missing list output for G_PRINT_DIVIDERS"); if (!scad.includes('[ G_LID_THICKNESS, 2.5 ]')) throw new Error("missing number output for G_LID_THICKNESS"); return "mixed-type outputs emitted"; })()
