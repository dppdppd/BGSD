wait app-root
new bit
js (() => { const inputs = document.querySelectorAll('.block-defaults input[value="all"]'); if (!inputs.length) throw new Error("block default controls not found"); inputs.forEach((input) => { if (!input.checked) input.click(); }); return `${inputs.length} block default controls set to all`; })()
js (() => { const row = document.querySelector('[data-testid="virtual-G_PRINT_TYPES"]'); if (!row) throw new Error("G_PRINT_TYPES row not found"); const button = document.querySelector('[data-testid="dynamic-ref-global-G_PRINT_TYPES"]'); if (!button) throw new Error("dynamic G_PRINT_TYPES selector not found"); if (!/All/.test(button.textContent || "")) throw new Error(`G_PRINT_TYPES should start as All, got ${button.textContent}`); if (row.querySelector("select.kv-type-select")) throw new Error("dynamic selector should replace generic mixed-type select"); return "dynamic print-type selector starts at All"; })()
shot mixed_type_bool_mode
js (async () => { const wait = () => new Promise((r) => setTimeout(r, 150)); const click = async (id) => { const el = document.querySelector(`[data-testid="${id}"]`); if (!el) throw new Error(`${id} not found`); el.click(); await wait(); }; await click("dynamic-ref-global-G_PRINT_TYPES"); await click("dynamic-ref-global-G_PRINT_TYPES-term-dividers"); let button = document.querySelector('[data-testid="dynamic-ref-global-G_PRINT_TYPES"]'); if (!button || !/DIVIDERS/.test(button.textContent || "")) throw new Error(`expected print-type selector to show DIVIDERS, got ${button?.textContent}`); let row = document.querySelector('[data-testid="virtual-G_LID_THICKNESS"]') || [...document.querySelectorAll(".line-row.kv")].find((candidate) => candidate.querySelector(".kv-key")?.textContent.trim() === "Lid Thickness"); let select = row?.querySelector("select.kv-type-select"); if (!select || select.value !== "false") throw new Error(`G_LID_THICKNESS should start false, got ${select?.value}`); select.value = "number"; select.dispatchEvent(new Event("change", { bubbles: true })); await wait(); row = document.querySelector('[data-testid="virtual-G_LID_THICKNESS"]') || [...document.querySelectorAll(".line-row.kv")].find((candidate) => candidate.querySelector(".kv-key")?.textContent.trim() === "Lid Thickness"); const input = row?.querySelector("input.kv-num"); if (!input) throw new Error("number mode numeric input not found"); input.value = "2.5"; input.dispatchEvent(new Event("change", { bubbles: true })); await wait(); return "dynamic print-type selector and number-or-false control update"; })()
shot mixed_type_list_mode
scad
js (() => { const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ""; if (!scad.includes('[ G_PRINT_TYPES, DIVIDERS ]')) throw new Error("missing selected divider output for G_PRINT_TYPES"); if (!scad.includes('[ G_LID_THICKNESS, 2.5 ]')) throw new Error("missing number output for G_LID_THICKNESS"); return "mixed-type outputs emitted"; })()
