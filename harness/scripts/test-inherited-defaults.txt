wait app-root
new ctd
intent "Virtual defaults inherit through the CTD hierarchy: Scene → Tray → Counter Set, with the nearest explicit value winning."
js (() => { if (document.querySelector('.global-overrides-label, [data-testid^="global-overrides-group-"]')) throw new Error('Global Overrides grouping must not be rendered'); return 'no special scene grouping'; })()
js (async () => { const body = () => document.querySelector('.line-row.struct.open .scene-name-input')?.closest('.line-row.struct.open')?.nextElementSibling; const row = (key) => [...body().querySelectorAll('.line-row.kv')].find((el) => el.querySelector('.kv-key')?.textContent?.trim() === key); const set = async (key, index, value) => { const control = row(key).querySelectorAll('input, select')[index]; control.value = value; control.dispatchEvent(new Event('change', { bubbles: true })); await new Promise((resolve) => setTimeout(resolve, 100)); }; await set('Tray Dimensions', 0, '120'); await set('Tray Dimensions', 1, '90'); await set('Shape', 0, 'SHAPE_HEX'); await set('Post Length Fraction', 0, '0.4'); return 'scene values set'; })()
js (() => { const block = (name) => { const row = [...document.querySelectorAll('.line-row.struct.open')].find((el) => el.querySelector('.struct-label')?.textContent?.trim() === name); if (!row) throw new Error(`${name} block not found`); return row.nextElementSibling; }; const parameter = (container, key) => [...container.querySelectorAll('.line-row.kv')].find((el) => el.querySelector('.kv-key')?.textContent?.trim() === key); const values = (container, key) => [...parameter(container, key).querySelectorAll('input, select')].map((el) => el.type === 'checkbox' ? el.checked : el.value); const tray = block('Tray'); const set = block('Counter Set'); const trayDims = values(tray, 'Tray Dimensions'); const trayShape = values(tray, 'Shape'); const setShape = values(set, 'Shape'); const setPost = values(set, 'Post Length Fraction'); if (JSON.stringify(trayDims) !== JSON.stringify(['120', '90'])) throw new Error(`Tray dimensions did not inherit: ${JSON.stringify(trayDims)}`); if (trayShape[0] !== 'SHAPE_HEX' || setShape[0] !== 'SHAPE_HEX') throw new Error(`Scene shape did not propagate: tray=${trayShape[0]} set=${setShape[0]}`); if (setPost[0] !== '0.4') throw new Error(`Post length fraction did not propagate: ${setPost[0]}`); if (!parameter(tray, 'Shape').querySelector('.kv-key')?.title.includes('Inherited from Scene')) throw new Error('Tray inheritance source is not Scene'); return { trayDims, trayShape, setShape, setPost }; })()
shot ctd_scene_defaults_propagated
js (async () => { const open = [...document.querySelectorAll('.line-row.struct.open')].find((el) => el.querySelector('.struct-label')?.textContent?.trim() === 'Tray'); const row = [...open.nextElementSibling.querySelectorAll('.line-row.kv')].find((el) => el.querySelector('.kv-key')?.textContent?.trim() === 'Shape'); const select = row.querySelector('select'); select.value = 'SHAPE_CIRCLE'; select.dispatchEvent(new Event('change', { bubbles: true })); await new Promise((resolve) => setTimeout(resolve, 100)); const setOpen = [...document.querySelectorAll('.line-row.struct.open')].find((el) => el.querySelector('.struct-label')?.textContent?.trim() === 'Counter Set'); const setRow = [...setOpen.nextElementSibling.querySelectorAll('.line-row.kv')].find((el) => el.querySelector('.kv-key')?.textContent?.trim() === 'Shape'); if (setRow.querySelector('select')?.value !== 'SHAPE_CIRCLE') throw new Error('Tray value did not override Scene for Counter Set'); if (!setRow.querySelector('.kv-key')?.title.includes('Inherited from Tray')) throw new Error('Counter Set inheritance source is not Tray'); return 'tray value propagated to counter set'; })()
shot ctd_tray_default_propagated
js (async () => { const open = [...document.querySelectorAll('.line-row.struct.open')].find((el) => el.querySelector('.struct-label')?.textContent?.trim() === 'Counter Set'); const row = [...open.nextElementSibling.querySelectorAll('.line-row.kv')].find((el) => el.querySelector('.kv-key')?.textContent?.trim() === 'Shape'); const select = row.querySelector('select'); select.value = 'SHAPE_SQUARE'; select.dispatchEvent(new Event('change', { bubbles: true })); await new Promise((resolve) => setTimeout(resolve, 100)); const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ''; const count = (scad.match(/\[ COUNTER_SHAPE,/g) || []).length; if (count !== 3) throw new Error(`Schema default must materialize below inherited Tray value; found ${count} shape entries`); return 'counter set local value materialized'; })()
js (async () => { const open = [...document.querySelectorAll('.line-row.struct.open')].find((el) => el.querySelector('.struct-label')?.textContent?.trim() === 'Counter Set'); const row = [...open.nextElementSibling.querySelectorAll('.line-row.kv')].find((el) => el.querySelector('.kv-key')?.textContent?.trim() === 'Shape'); const select = row.querySelector('select'); select.value = 'SHAPE_CIRCLE'; select.dispatchEvent(new Event('change', { bubbles: true })); await new Promise((resolve) => setTimeout(resolve, 100)); const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ''; const count = (scad.match(/\[ COUNTER_SHAPE,/g) || []).length; if (count !== 2) throw new Error(`Returning to the Tray value must remove the local override; found ${count} shape entries`); return 'counter set returned to tray value'; })()
js (async () => { const open = [...document.querySelectorAll('.line-row.struct.open')].find((el) => el.querySelector('.struct-label')?.textContent?.trim() === 'Tray'); const row = [...open.nextElementSibling.querySelectorAll('.line-row.kv')].find((el) => el.querySelector('.kv-key')?.textContent?.trim() === 'Shape'); const reset = row.querySelector('.delete-btn'); if (!reset || reset.title !== 'Use Scene value') throw new Error(`Expected Use Scene value reset, found ${reset?.title}`); reset.click(); await new Promise((resolve) => setTimeout(resolve, 100)); const setOpen = [...document.querySelectorAll('.line-row.struct.open')].find((el) => el.querySelector('.struct-label')?.textContent?.trim() === 'Counter Set'); const setRow = [...setOpen.nextElementSibling.querySelectorAll('.line-row.kv')].find((el) => el.querySelector('.kv-key')?.textContent?.trim() === 'Shape'); if (setRow.querySelector('select')?.value !== 'SHAPE_HEX') throw new Error('Removing Tray value did not restore propagated Scene value'); const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ''; const count = (scad.match(/\[ COUNTER_SHAPE,/g) || []).length; if (count !== 1) throw new Error(`Expected only Scene shape after reset; found ${count}`); return 'scene value restored through lower layers'; })()
shot ctd_nearest_layer_restored
scad
new bit
js (() => { if (document.querySelector('.global-overrides-label, [data-testid^="global-overrides-group-"]')) throw new Error('BIT must not render a Global Overrides grouping'); return 'BIT has no special scene grouping'; })()
shot bit_without_global_group
