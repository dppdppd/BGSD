wait app-root
new bit
js (() => { const inputs = document.querySelectorAll('.block-defaults input[value="all"]'); if (!inputs.length) throw new Error("block default controls not found"); inputs.forEach((input) => { if (!input.checked) input.click(); }); return `${inputs.length} block default controls set to all`; })()
shot print_group_defaults
js (async () => { const wait = () => new Promise((r) => setTimeout(r, 150)); const rowsByLabel = (label) => [...document.querySelectorAll(".line-row.kv")].filter((row) => row.querySelector(".kv-key")?.textContent.trim() === label); const rowFor = (id, occurrence) => document.querySelector(`[data-testid="${id}"]`) || rowsByLabel("Print Group")[occurrence]; const setRow = async (id, occurrence, value) => { let row = rowFor(id, occurrence); if (!row) throw new Error(`${id} row not found`); const select = row.querySelector("select.kv-type-select"); if (select && select.value !== "text") { select.value = "text"; select.dispatchEvent(new Event("change", { bubbles: true })); await wait(); row = rowFor(id, occurrence); } const input = row?.querySelector("input.kv-str"); if (!input) throw new Error(`${id} input not found`); input.value = value; input.dispatchEvent(new Event("change", { bubbles: true })); await wait(); }; const click = async (id) => { const el = document.querySelector(`[data-testid="${id}"]`); if (!el) throw new Error(`${id} not found`); el.click(); await wait(); }; await setRow("virtual-PRINT_GROUP", 1, "red"); await setRow("virtual-lid-PRINT_GROUP", 2, "red-lid"); await click("dynamic-ref-global-G_PRINT_GROUP"); await click("dynamic-ref-global-G_PRINT_GROUP-term-red"); return "print groups set"; })()
shot print_group_values
scad
js (() => { const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ""; if (!/boardgame_insert_toolkit_lib\.\d+(?:\.\d+){0,2}\.scad/.test(scad)) throw new Error("new BIT project did not use a BIT library include"); if (!scad.includes("[ G_PRINT_GROUP, \"red\" ]")) throw new Error("missing G_PRINT_GROUP output"); if (!scad.includes("[ PRINT_GROUP, \"red\" ]")) throw new Error("missing box PRINT_GROUP output"); if (!scad.includes("[ PRINT_GROUP, \"red-lid\" ]")) throw new Error("missing lid PRINT_GROUP output"); return "BIT print groups emitted"; })()
