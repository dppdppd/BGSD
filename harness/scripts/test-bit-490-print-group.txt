wait app-root
new bit
click view-menu-button
click view-defaults-all
shot print_group_defaults
js (async () => { const setRow = (id, value) => { const row = document.querySelector(`[data-testid="${id}"]`); if (!row) throw new Error(`${id} row not found`); const input = row.querySelector("input"); if (!input) throw new Error(`${id} input not found`); input.value = value; input.dispatchEvent(new Event("change", { bubbles: true })); }; setRow("virtual-G_PRINT_GROUP", "red"); setRow("virtual-PRINT_GROUP", "red"); await new Promise((r) => setTimeout(r, 200)); return "print groups set"; })()
shot print_group_values
scad
js (() => { const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ""; if (!scad.includes("boardgame_insert_toolkit_lib.4.9.0.scad")) throw new Error("new BIT project did not use 4.9.0"); if (!scad.includes("[ G_PRINT_GROUP, \"red\" ]")) throw new Error("missing G_PRINT_GROUP output"); if (!scad.includes("[ PRINT_GROUP, \"red\" ]")) throw new Error("missing PRINT_GROUP output"); return "BIT 4.9.0 print groups emitted"; })()
