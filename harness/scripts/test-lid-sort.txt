wait app-root
new bit
ipc menu-defaults-mode "all"
js (async () => { await new Promise(r => setTimeout(r, 400)); })()
js document.querySelector('[data-testid="content-area"]').scrollTop = 99999
shot 01_show_all_virtual_lid
js (() => { const rows = [...document.querySelectorAll('[data-testid^="virtual-lid-"]')]; return rows.map(r => r.getAttribute('data-testid').replace('virtual-lid-','')); })()
js (() => { const r = document.querySelector('[data-testid="virtual-lid-LID_HEIGHT"]'); if (!r) return 'no LID_HEIGHT'; const inp = r.querySelector('input.kv-num'); inp.value = '8'; inp.dispatchEvent(new Event('change', { bubbles: true })); return 'set 8'; })()
js (async () => { await new Promise(r => setTimeout(r, 500)); })()
js document.querySelector('[data-testid="content-area"]').scrollTop = 99999
shot 02_after_materialize
js (() => { const rows = [...document.querySelectorAll('div.line-row.kv')]; return rows.map(r => { const k = r.querySelector('.kv-key'); return k ? k.textContent.trim() : null; }).filter(t => t && /^(Debug|Cutout|Fit|Height|Inset|Invert|Label|Pattern|Row|Stripe|Solid|Tabs|Column)/.test(t)); })()
