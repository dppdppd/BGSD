wait app-root
new bit
js (async () => { const add = async (title) => { for (const trigger of [...document.querySelectorAll('button.add-menu-trigger')]) { trigger.click(); await new Promise((r) => setTimeout(r, 50)); const item = [...document.querySelectorAll('button.add-menu-item')].find((b) => b.title === title); if (item) { item.click(); await new Promise((r) => setTimeout(r, 200)); return; } trigger.click(); } throw new Error(`${title} item not found`); }; await add('Add BOX_FEATURE block'); })()
shot feature-added
js (() => { const labels = [...document.querySelectorAll('.line-row.struct.open .struct-label')].filter(l => /FEATURE/i.test(l.textContent || '')); if (!labels.length) throw new Error('no FEATURE label'); window._featBtn = labels[0].closest('.line-row').querySelector('.collapse-btn'); window._featBtn.click(); return 'clicked'; })()
js new Promise(r => setTimeout(r, 50))
shot feat-50ms
js new Promise(r => setTimeout(r, 50))
shot feat-100ms
js new Promise(r => setTimeout(r, 50))
shot feat-150ms
js new Promise(r => setTimeout(r, 100))
shot feat-after
js window._featBtn.click()
js new Promise(r => setTimeout(r, 100))
shot feat-expanded-100ms
js new Promise(r => setTimeout(r, 200))
shot feat-fully-expanded
