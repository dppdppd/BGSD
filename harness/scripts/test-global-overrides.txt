wait app-root
new ctd
intent "Global Overrides groups the scene settings; Tray Dimensions and Counter Set defaults show their inherited effective values."
js (() => { const groups = [...document.querySelectorAll('[data-testid^="global-overrides-group-"]')]; if (groups.length !== 1) throw new Error(`Expected one Global Overrides group, found ${groups.length}`); if (groups[0].querySelector('.global-overrides-label')?.textContent?.trim() !== 'Global Overrides') throw new Error('Global Overrides label missing'); return 'CTD scene grouping present'; })()
js (async () => { const body = () => document.querySelector('.line-row.struct.open .scene-name-input')?.closest('.line-row.struct.open')?.nextElementSibling; const row = (key) => [...body().querySelectorAll('.line-row.kv')].find((el) => el.querySelector('.kv-key')?.textContent?.trim() === key); const set = async (key, index, value) => { const control = row(key).querySelectorAll('input, select')[index]; control.value = value; control.dispatchEvent(new Event('change', { bubbles: true })); await new Promise((resolve) => setTimeout(resolve, 100)); }; await set('Tray Dimensions', 0, '120'); await set('Tray Dimensions', 1, '90'); await set('Shape', 0, 'SHAPE_HEX'); await set('Post Length Fraction', 0, '0.4'); return 'scene overrides set'; })()
js (() => { const block = (name) => { const row = [...document.querySelectorAll('.line-row.struct.open')].find((el) => el.querySelector('.struct-label')?.textContent?.trim() === name); if (!row) throw new Error(`${name} block not found`); return row.nextElementSibling; }; const values = (container, key) => { const row = [...container.querySelectorAll('.line-row.kv')].find((el) => el.querySelector('.kv-key')?.textContent?.trim() === key); if (!row) throw new Error(`${key} row not found`); return [...row.querySelectorAll('input, select')].map((el) => el.type === 'checkbox' ? el.checked : el.value); }; const tray = block('Tray'); const set = block('Counter Set'); const trayDims = values(tray, 'Tray Dimensions'); const trayShape = values(tray, 'Shape'); const setShape = values(set, 'Shape'); const setPost = values(set, 'Post Length Fraction'); if (JSON.stringify(trayDims) !== JSON.stringify(['120', '90'])) throw new Error(`Tray dimensions did not inherit: ${JSON.stringify(trayDims)}`); if (trayShape[0] !== 'SHAPE_HEX' || setShape[0] !== 'SHAPE_HEX') throw new Error(`Counter shape did not inherit: tray=${trayShape[0]} set=${setShape[0]}`); if (setPost[0] !== '0.4') throw new Error(`Post length fraction did not inherit: ${setPost[0]}`); return { trayDims, trayShape, setShape, setPost }; })()
shot ctd_inherited_values
js (async () => { const open = [...document.querySelectorAll('.line-row.struct.open')].find((el) => el.querySelector('.struct-label')?.textContent?.trim() === 'Counter Set'); const row = [...open.nextElementSibling.querySelectorAll('.line-row.kv')].find((el) => el.querySelector('.kv-key')?.textContent?.trim() === 'Shape'); const select = row.querySelector('select'); select.value = 'SHAPE_SQUARE'; select.dispatchEvent(new Event('change', { bubbles: true })); await new Promise((resolve) => setTimeout(resolve, 100)); const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ''; const count = (scad.match(/\[ COUNTER_SHAPE,/g) || []).length; if (count !== 2) throw new Error(`Schema default must materialize as a local override; found ${count} shape entries`); return 'local schema-default override materialized'; })()
shot ctd_local_override
js (async () => { const open = [...document.querySelectorAll('.line-row.struct.open')].find((el) => el.querySelector('.struct-label')?.textContent?.trim() === 'Counter Set'); const row = [...open.nextElementSibling.querySelectorAll('.line-row.kv')].find((el) => el.querySelector('.kv-key')?.textContent?.trim() === 'Shape'); const select = row.querySelector('select'); select.value = 'SHAPE_HEX'; select.dispatchEvent(new Event('change', { bubbles: true })); await new Promise((resolve) => setTimeout(resolve, 100)); const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ''; const count = (scad.match(/\[ COUNTER_SHAPE,/g) || []).length; if (count !== 1) throw new Error(`Returning to the inherited value must remove the local override; found ${count} shape entries`); return 'inherited value dematerialized local override'; })()
shot ctd_inherited_restored
scad
new bit
js (() => { const groups = [...document.querySelectorAll('[data-testid^="global-overrides-group-"]')]; if (groups.length !== 1 || groups[0].querySelector('.global-overrides-label')?.textContent?.trim() !== 'Global Overrides') throw new Error('BIT Global Overrides grouping missing'); return 'BIT scene grouping present'; })()
shot bit_global_overrides_group
