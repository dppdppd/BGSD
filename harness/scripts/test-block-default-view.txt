wait app-root
new bit
shot block_defaults_favorites
js (async () => { const wait = () => new Promise((r) => setTimeout(r, 100)); const button = document.querySelector('[data-testid="view-menu-button"]'); if (!button) throw new Error("View menu button not found"); button.click(); await wait(); if (document.querySelector('[data-testid^="view-defaults-"]')) throw new Error("file-wide defaults controls should not be in the View menu"); if (!document.querySelector('[data-testid="view-show-scad"]')) throw new Error("View menu should still expose Show SCAD"); button.click(); return "View menu only exposes SCAD visibility"; })()
js (() => { const boxHeader = [...document.querySelectorAll('.line-row.struct.open')].find((row) => /box/i.test(row.querySelector('.struct-label')?.textContent || "") && row.querySelector('.block-defaults input[value="none"]')); if (!boxHeader) throw new Error("Box header not found"); const none = boxHeader.querySelector('.block-defaults input[value="none"]'); if (!none) throw new Error("Box defaults None radio not found"); none.click(); return "Box defaults hidden"; })()
js (() => { if (document.querySelector('[data-testid="virtual-ENABLED_B"]')) throw new Error("Box default key should be hidden when the box is set to None"); const dataHeader = [...document.querySelectorAll('.line-row.struct.open')].find((row) => row.querySelector('.scene-name-input')); if (!dataHeader) throw new Error("Data header not found"); const dataFavorites = dataHeader.querySelector('.block-defaults input[value="favorites"]'); if (!dataFavorites?.checked) throw new Error("Data block default mode should remain Favorites"); return "per-block None only hides box defaults"; })()
shot block_defaults_box_none
js (() => { const boxHeader = [...document.querySelectorAll('.line-row.struct.open')].find((row) => /box/i.test(row.querySelector('.struct-label')?.textContent || "") && row.querySelector('.block-defaults input[value="all"]')); if (!boxHeader) throw new Error("Box header not found"); const all = boxHeader.querySelector('.block-defaults input[value="all"]'); if (!all) throw new Error("Box defaults All radio not found"); all.click(); return "Box defaults set to all"; })()
js (() => { if (!document.querySelector('[data-testid="virtual-CHAMFER_N"]')) throw new Error("Box default key CHAMFER_N should be visible when the box is set to All"); return "per-block All exposes hidden box defaults"; })()
shot block_defaults_box_all
