wait app-root
new bit
js (() => { const inputs = document.querySelectorAll('.block-defaults input[value="all"]'); if (!inputs.length) throw new Error("block default controls not found"); inputs.forEach((input) => { if (!input.checked) input.click(); }); return `${inputs.length} block default controls set to all`; })()
js (async () => { const wait = () => new Promise((r) => setTimeout(r, 150)); const rowsByLabel = (label) => [...document.querySelectorAll(".line-row.kv")].filter((row) => row.querySelector(".kv-key")?.textContent.trim() === label); const rowFor = (id, occurrence) => document.querySelector(`[data-testid="${id}"]`) || rowsByLabel("Print Group")[occurrence]; const setText = async (id, occurrence, value) => { let row = rowFor(id, occurrence); if (!row) throw new Error(`${id} row not found`); const select = row.querySelector("select.kv-type-select"); if (select && select.value !== "text") { select.value = "text"; select.dispatchEvent(new Event("change", { bubbles: true })); await wait(); row = rowFor(id, occurrence); } const input = row?.querySelector("input.kv-str"); if (!input) throw new Error(`${id} input not found`); input.value = value; input.dispatchEvent(new Event("change", { bubbles: true })); await wait(); }; await setText("virtual-PRINT_GROUP", 1, "alpha"); await setText("virtual-lid-PRINT_GROUP", 2, "lid"); return "created print group terms"; })()
js (async () => { const wait = () => new Promise((r) => setTimeout(r, 100)); const click = async (id) => { const el = document.querySelector(`[data-testid="${id}"]`); if (!el) throw new Error(`${id} not found`); el.click(); await wait(); }; const btn = document.querySelector('[data-testid="dynamic-ref-global-G_PRINT_GROUPS"]'); if (!btn) throw new Error("G_PRINT_GROUPS dynamic button missing"); if (!/All/.test(btn.textContent || "")) throw new Error("G_PRINT_GROUPS should start All"); await click("dynamic-ref-global-G_PRINT_GROUPS"); await click("dynamic-ref-global-G_PRINT_GROUPS-term-alpha"); await click("dynamic-ref-global-G_PRINT_GROUPS-term-lid"); return "selected alpha and lid"; })()
shot dynamic_print_group_menu
js (async () => { const wait = () => new Promise((r) => setTimeout(r, 100)); const click = async (id) => { const el = document.querySelector(`[data-testid="${id}"]`); if (!el) throw new Error(`${id} not found`); el.click(); await wait(); }; await click("dynamic-ref-global-G_PRINT_BOXES"); await click("dynamic-ref-global-G_PRINT_BOXES-term-box-1"); return "selected box 1"; })()
shot dynamic_box_selector
scad
js (() => { const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ""; if (!scad.includes('[ G_PRINT_GROUPS, ["alpha", "lid"] ]')) throw new Error("missing G_PRINT_GROUPS list output"); if (!scad.includes('[ G_PRINT_BOXES, "box 1" ]')) throw new Error("missing print boxes output"); return "dynamic selector SCAD emitted"; })()
