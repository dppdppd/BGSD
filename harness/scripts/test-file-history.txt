wait app-root
new bit
wait content-area
wait file-menu-button
intent "Open the File menu and show the Recent Files flyout"
click file-menu-button
js document.querySelector('[data-testid="menu-recent-files"]').focus()
shot file_menu_recent
js document.dispatchEvent(new KeyboardEvent('keydown', {key:'Escape', code:'Escape', bubbles:true, cancelable:true}))
intent "Open the View menu and show the SCAD toggle"
click view-menu-button
shot view_menu_show_scad
js document.dispatchEvent(new KeyboardEvent('keydown', {key:'Escape', code:'Escape', bubbles:true, cancelable:true}))
intent "Create two Version History entries from the File menu and Ctrl+S"
js const nameInput = document.querySelector('[data-testid="line-4"] input.kv-str'); nameInput.value = 'history one'; nameInput.dispatchEvent(new Event('change', {bubbles:true}))
click file-menu-button
click menu-save-version
js (async () => { await new Promise(r => setTimeout(r, 500)); })()
js const nameInput2 = document.querySelector('[data-testid="line-4"] input.kv-str'); nameInput2.value = 'history two'; nameInput2.dispatchEvent(new Event('change', {bubbles:true}))
js document.dispatchEvent(new KeyboardEvent('keydown', {key:'s', code:'KeyS', ctrlKey:true, bubbles:true, cancelable:true}))
wait save-toast
shot save_version_toast
click file-menu-button
click file-history-button
wait file-history-modal
shot history_list
intent "Click the previous Version History row to preview it"
js document.querySelectorAll('[data-testid="history-row"]')[1].click()
js (async () => { await new Promise(r => setTimeout(r, 250)); })()
shot history_preview
intent "Click the same Version History row again to close the preview"
js document.querySelectorAll('[data-testid="history-row"]')[1].click()
js (async () => { await new Promise(r => setTimeout(r, 250)); })()
shot history_preview_closed
js document.querySelectorAll('[data-testid="history-row"]')[1].click()
js (async () => { await new Promise(r => setTimeout(r, 250)); })()
intent "Open the previous saved version in OpenSCAD"
js document.querySelectorAll('[data-testid="history-preview-button"]')[1].click()
shot history_openscad_version
intent "Label and pin the previous saved version"
js document.querySelectorAll('[data-testid="history-label-button"]')[1].click()
type history-label-input "before second edit"
click history-label-save
js document.querySelectorAll('[data-testid="history-pin-button"]')[1].click()
shot history_labeled_pinned
intent "Restore the previous version"
js document.querySelectorAll('[data-testid="history-restore-button"]')[1].click()
shot history_restored
scad
