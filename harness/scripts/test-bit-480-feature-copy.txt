wait app-root
new bit
js (() => { const inputs = document.querySelectorAll('.block-defaults input[value="all"]'); if (!inputs.length) throw new Error("block default controls not found"); inputs.forEach((input) => { if (!input.checked) input.click(); }); return `${inputs.length} block default controls set to all`; })()
js document.querySelector('[data-testid="content-area"]').scrollTop = 99999
shot before_copy
js (async () => { const add = async (title) => { for (const trigger of [...document.querySelectorAll('button.add-menu-trigger')]) { trigger.click(); await new Promise((r) => setTimeout(r, 50)); const item = [...document.querySelectorAll('button.add-menu-item')].find((b) => b.title === title); if (item) { item.click(); await new Promise((r) => setTimeout(r, 200)); return; } trigger.click(); } throw new Error(`${title} item not found`); }; await add('Add FEATURE_COPY block'); })()
js (() => { const label = [...document.querySelectorAll('.line-row.struct.open .struct-label')].find((el) => /Copy/.test(el.textContent || '')); if (!label) throw new Error('FEATURE_COPY label not found after add'); label.scrollIntoView({ block: 'center' }); return label.textContent.trim(); })()
shot copy_added
scad
js (() => { const scad = document.querySelector('[data-testid="scad-output"]')?.textContent || ""; if (!scad.includes("boardgame_insert_toolkit_lib.4.9.1.scad")) throw new Error("new BIT project did not use 4.9.1"); if (!scad.includes("[ FEATURE_COPY,")) throw new Error("missing FEATURE_COPY block"); if (!scad.includes("[ FEATURE_REFERENCE, \"\" ]")) throw new Error("missing FEATURE_REFERENCE starter field"); return "BIT 4.9.1 copy block emitted"; })()
