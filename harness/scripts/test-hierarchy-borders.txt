wait app-root
intent "Expanded BIT hierarchies have continuous borders on all four sides at every nesting depth."
new bit
js (() => { const rows = [...document.querySelectorAll('.line-row.struct.open')]; if (rows.length < 3) throw new Error(`expected nested BIT blocks, found ${rows.length}`); const nested = rows.find((row) => Number.parseInt(getComputedStyle(row).getPropertyValue('--depth'), 10) > 0); if (!nested) throw new Error('nested BIT hierarchy not found'); const before = getComputedStyle(nested, '::before'); const after = getComputedStyle(nested, '::after'); const capVisible = after.backgroundImage !== 'none' || after.backgroundColor !== 'rgba(0, 0, 0, 0)'; if (before.backgroundImage === 'none' || !capVisible) throw new Error('hierarchy frame layers are missing'); if (!getComputedStyle(nested).getPropertyValue('--frame-right').trim()) throw new Error('nested right frame offset is missing'); return `${rows.length} BIT hierarchy headers expose complete frame layers`; })()
js document.querySelector('[data-testid="content-area"]').scrollTop = 0
shot bit_expanded_frames
intent "A collapsed BIT hierarchy remains a complete bordered rectangle."
js (async () => { const row = [...document.querySelectorAll('.line-row.struct.open')].find((el) => /Lid/.test(el.textContent || '') && el.querySelector('.collapse-btn')); if (!row) throw new Error('collapsible Lid hierarchy not found'); row.querySelector('.collapse-btn').click(); await new Promise((resolve) => setTimeout(resolve, 250)); if (!row.classList.contains('collapsed')) throw new Error('Lid hierarchy did not collapse'); const after = getComputedStyle(row, '::after'); if (after.bottom !== '0px' || after.backgroundImage.split('linear-gradient').length < 3) throw new Error('collapsed hierarchy is missing a bottom frame cap'); row.scrollIntoView({ block: 'center' }); return 'collapsed Lid keeps top and bottom frame caps'; })()
shot bit_collapsed_frame
intent "Expanded CTD scene, tray, counter-set, and lid hierarchies are bordered all the way around."
new ctd
js (() => { const rows = [...document.querySelectorAll('.line-row.struct.open')]; if (rows.length < 4) throw new Error(`expected CTD scene/tray/counter/lid blocks, found ${rows.length}`); const depths = rows.map((row) => Number.parseInt(getComputedStyle(row).getPropertyValue('--depth'), 10)); if (!depths.some((depth) => depth >= 2)) throw new Error(`expected a depth-2 CTD hierarchy, got ${depths.join(',')}`); for (const row of rows) { const after = getComputedStyle(row, '::after'); if (after.backgroundImage === 'none' && after.backgroundColor === 'rgba(0, 0, 0, 0)') throw new Error(`missing frame cap for ${row.textContent?.trim()}`); } return `${rows.length} CTD hierarchy headers expose complete frame caps`; })()
js document.querySelector('[data-testid="content-area"]').scrollTop = 0
shot ctd_expanded_frames
