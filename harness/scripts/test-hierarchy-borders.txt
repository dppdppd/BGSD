wait app-root
intent "Expanded BIT hierarchies keep continuous left, top, and bottom borders while leaving the favorite-star side open."
new bit
js (() => { const rows = [...document.querySelectorAll('.line-row.struct.open')]; if (rows.length < 3) throw new Error(`expected nested BIT blocks, found ${rows.length}`); const nested = rows.find((row) => Number.parseInt(getComputedStyle(row).getPropertyValue('--depth'), 10) > 0); if (!nested) throw new Error('nested BIT hierarchy not found'); const style = getComputedStyle(nested); const before = getComputedStyle(nested, '::before'); const after = getComputedStyle(nested, '::after'); const capVisible = after.backgroundImage !== 'none' || after.backgroundColor !== 'rgba(0, 0, 0, 0)'; if (before.backgroundImage === 'none' || !capVisible) throw new Error('hierarchy bracket layers are missing'); if ((style.backgroundImage.match(/linear-gradient/g) || []).length !== 2) throw new Error(`expected left edge plus row fill only, got ${style.backgroundImage}`); if ((before.backgroundImage.match(/repeating-linear-gradient/g) || []).length !== 1) throw new Error(`expected left ancestor rails only, got ${before.backgroundImage}`); if (style.getPropertyValue('--ancestor-right-width').trim()) throw new Error('right ancestor rail is still configured'); return `${rows.length} BIT hierarchy headers expose three-sided brackets`; })()
js (() => { const category = document.querySelector('.line-row.kv-category'); if (!category) throw new Error('BIT parameter category divider not found'); const style = getComputedStyle(category); const divider = getComputedStyle(category, '::after'); if (style.marginTop !== '0px' || style.marginBottom !== '0px') throw new Error(`parameter category retains an unlabeled gap: ${style.marginTop}/${style.marginBottom}`); if (!category.querySelector('.category-label') || divider.backgroundColor === 'rgba(0, 0, 0, 0)') throw new Error('labeled category divider is missing'); return 'BIT parameter groups use only the labeled divider'; })()
js document.querySelector('[data-testid="content-area"]').scrollTop = 0
shot bit_expanded_frames
intent "A collapsed BIT hierarchy keeps both horizontal caps and an open right side."
js (async () => { const row = [...document.querySelectorAll('.line-row.struct.open')].find((el) => /Lid/.test(el.textContent || '') && el.querySelector('.collapse-btn')); if (!row) throw new Error('collapsible Lid hierarchy not found'); row.querySelector('.collapse-btn').click(); await new Promise((resolve) => setTimeout(resolve, 250)); if (!row.classList.contains('collapsed')) throw new Error('Lid hierarchy did not collapse'); const after = getComputedStyle(row, '::after'); if (after.bottom !== '0px' || after.backgroundImage.split('linear-gradient').length < 3) throw new Error('collapsed hierarchy is missing a bottom frame cap'); row.scrollIntoView({ block: 'center' }); return 'collapsed Lid keeps top and bottom frame caps'; })()
shot bit_collapsed_frame
intent "Expanded CTD scene, tray, counter-set, and lid hierarchies use three-sided brackets that stay clear of stars."
new ctd
js (() => { const rows = [...document.querySelectorAll('.line-row.struct.open')]; if (rows.length < 4) throw new Error(`expected CTD scene/tray/counter/lid blocks, found ${rows.length}`); const depths = rows.map((row) => Number.parseInt(getComputedStyle(row).getPropertyValue('--depth'), 10)); if (!depths.some((depth) => depth >= 2)) throw new Error(`expected a depth-2 CTD hierarchy, got ${depths.join(',')}`); for (const row of rows) { const style = getComputedStyle(row); const after = getComputedStyle(row, '::after'); if (after.backgroundImage === 'none' && after.backgroundColor === 'rgba(0, 0, 0, 0)') throw new Error(`missing bracket cap for ${row.textContent?.trim()}`); if ((style.backgroundImage.match(/linear-gradient/g) || []).length !== 2) throw new Error(`right edge remains on ${row.textContent?.trim()}`); } return `${rows.length} CTD hierarchy headers expose open-right brackets`; })()
js (() => { const categories = [...document.querySelectorAll('.line-row.kv-category')]; if (!categories.length) throw new Error('CTD parameter category dividers not found'); for (const category of categories) { const style = getComputedStyle(category); if (style.marginTop !== '0px' || style.marginBottom !== '0px') throw new Error(`CTD category retains an unlabeled gap: ${style.marginTop}/${style.marginBottom}`); } return `${categories.length} CTD parameter groups use only labeled dividers`; })()
js document.querySelector('[data-testid="content-area"]').scrollTop = 0
shot ctd_expanded_frames
